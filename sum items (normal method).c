#include<stdio.h>
int main()
{
   int item = 100, sum = 0;

   sum = sum + item;
     printf("Sum1 is: %d\n", sum);

   sum = sum + item;
      printf("Sum2 is: %d\n", sum);

   sum = sum + item;
      printf("Sum3 is: %d\n", sum);

   sum = sum + item;
      printf("Sum4 is: %d\n", sum);

   sum = sum + item;
   printf("Final sum is: %d", sum);

 return 0;

}
